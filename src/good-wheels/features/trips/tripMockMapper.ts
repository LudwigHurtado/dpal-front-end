import type { Trip } from './tripTypes';

/**
 * Mock/API mapper placeholder.
 * Kept as a separate file because real backend shapes often differ from UI domain shapes.
 */
export function mapMockTripToTrip(input: Trip): Trip {
  return input;
}

